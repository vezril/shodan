{
  "name": "@shodan/daemon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "shodan recon daemon (engine) — owns the radios, runs on the Linux box",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@shodan/contract": "*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
